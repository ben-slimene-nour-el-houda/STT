def enrich_with_llm(text: str) -> str:
    """Stub LLM enrichment.
    In production this would call a local or remote LLM to improve the transcription.
    Here we simply return the original text unchanged.
    """
    return text
